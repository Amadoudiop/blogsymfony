{% extends 'base.html.twig' %}

{% block body %}
    <h1>Statusmoderator</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statusModerator.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ statusModerator.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('statusmoderator_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('statusmoderator_edit', { 'id': statusModerator.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
